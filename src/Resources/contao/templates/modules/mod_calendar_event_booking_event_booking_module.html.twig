{% extends '@Contao/block_unsearchable' %}

{% block content %}
    {% if messages is defined %}
        {{ messages|raw }}
    {% endif %}

    {% if form is defined %}
        {% if canRegister.invoke() == true %}
            {{ form|raw }}
        {% endif %}

    {% endif %}

{% endblock %}
